// Create Array
data = ["Kotlin"]

// Add Data (add method)
data.add("Python")

// Add Data (short syntax)
data[2] = "Java"

// Print Array
echo(data)

// Print Size
echo("size = " + !data.size())

// Get Element
echo("element at pos 0 = " + data[0])