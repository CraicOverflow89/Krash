// Basic Literals
person = {name: "James", age: 30, isMale: true, lang: ["Kotlin", "Python"]}
echo(person)

// TEMP DEBUG
echo("temp 1")

/*
 * Says Hello
 *
 * @param name
 * @returns void
 */
hello = fun(name = "Somebody") {
    echo("Hello " + name)
}

// TEMP DEBUG
echo("temp 2")

// Invoke Callable
hello(person["name"])

// TEMP DEBUG
echo("temp 3")

// IIFE
fun(){echo("IIFE")}()

// Simple Callable
simple = fun() = echo("simple callable")
simple()

// CWD Global
echo("cwd is " + $CWD, "user directory is " + $HOME)

// Callable Scopes
/*
x = 1
fun() {
    x = 2
    echo("inside first callable: " + !x)
    fun() {
        x = 3
        echo("inside second callable: " + !x)
    }()
}()
*/

// Ref Persist
/*
list = [0, 1, 2]
first_value = list[0]
first_ref = &list[0]
list = [3, 4, 5]
echo(first_value, first_ref)
*/